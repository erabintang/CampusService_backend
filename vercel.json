{
  "functions": {
    "api/*.php": {
      "runtime": "vercel-php@0.7.4",
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "routes": [
    { "src": "/assets/(.*)", "dest": "/public/assets/$1" },
    { "src": "/storage/(.*)", "dest": "/public/storage/$1" },
    { "src": "/build/(.*)", "dest": "/public/build/$1" },
    { "src": "/favicon.ico", "dest": "/public/favicon.ico" },
    { "src": "/(.*)", "dest": "/api/index.php" }
  ],
  "installCommand": "composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist",
  "buildCommand": "mkdir -p storage/framework/cache/data storage/framework/sessions storage/framework/views storage/logs"
}
